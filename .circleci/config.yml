version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.9
    working_directory: /go/src/github.com/blockloop/boar
    steps:
      - checkout
      - run: env
      - run: go test -v -race ./...
      - run: go build -v
